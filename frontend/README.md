# Operations dashboard

React + Vite frontend for the Waste Management Optimizer API.

```powershell
cd frontend
npm install
npm run dev
```

The Vite proxy forwards `/api` to `http://127.0.0.1:8000`. Start the FastAPI server first.
