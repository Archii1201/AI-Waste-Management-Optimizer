# API origin for production / split hosting.
# Leave empty for local same-origin requests (`/api/v1` via the Vite proxy).
# Example: VITE_API_BASE_URL=https://your-api-domain.example.com
VITE_API_BASE_URL=
